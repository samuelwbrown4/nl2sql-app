ideas for improvement
    - put app behind user auth
        -could then add table to *not yet existing* db for user roles, i.e. a manufacturing could only ask questions on tables/fields that pertain to manufacturing etc.
    -allow for SHORT dialogue with LLM
        -if LLM asks a clarifying question, allow the user to respond to it instead of refactoring their original query. Limit this to something like 2 clarifications before user needs to rewrite original